# ===== Production Docker Compose =====
# 使用 GHCR 预构建镜像 + Cloudflare Tunnel 绕过备案限制
#
# 架构：用户 → Cloudflare CDN (SSL) → Tunnel → nginx:80 → backend/frontend
# nginx 不对外暴露端口，所有流量通过 Cloudflare Tunnel 进入
#
# 部署步骤：
#   1. 将此文件 + .env + nginx/ + cloudflared/ 复制到服务器
#   2. 配置 Cloudflare Tunnel（运行 scripts/cloudflare-tunnel-setup.sh）
#   3. 将 CF_TUNNEL_TOKEN 写入 .env
#   4. docker compose -f docker-compose.prod.yml pull
#   5. docker compose -f docker-compose.prod.yml up -d
#
# 更新步骤：
#   1. docker compose -f docker-compose.prod.yml pull
#   2. docker compose -f docker-compose.prod.yml up -d

services:
  db:
    image: postgres:16-alpine
    container_name: wiki-db
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-changeme}
      POSTGRES_DB: ${DB_NAME:-wiki}
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./backup:/backup
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres}"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  backend:
    image: ghcr.io/haplotes405/myblog-backend:latest
    container_name: wiki-backend
    # Backend 不对外暴露端口，仅通过 nginx 访问（如需调试可临时解注释）
    # ports:
    #   - "127.0.0.1:8000:8000"
    expose:
      - "8000"
    environment:
      PORT: "8000"
      JWT_SECRET: ${JWT_SECRET:-change-me-in-production}
      DATABASE_URL: postgresql://${DB_USER:-postgres}:${DB_PASSWORD:-changeme}@db:5432/${DB_NAME:-wiki}
      UPLOAD_DIR: ./uploads
      BLOGGER_EMAIL: ${BLOGGER_EMAIL:-admin@hapLotes405.wiki}
      BLOGGER_PASSWORD: ${BLOGGER_PASSWORD:-changeme}
      CORS_ORIGIN: ${CORS_ORIGIN:-http://localhost}
    volumes:
      - uploads:/app/uploads
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://localhost:8000/api/health', (r) => { process.exit(r.statusCode === 200 ? 0 : 1) })\""]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  frontend:
    image: ghcr.io/haplotes405/myblog-frontend:latest
    container_name: wiki-frontend
    # Frontend 不对外暴露端口，仅通过 nginx 访问
    # ports:
    #   - "127.0.0.1:3000:3000"
    expose:
      - "3000"
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ============================================================
  # Nginx 反向代理 —— 内部服务，不对外暴露端口
  # Cloudflare Tunnel 负责将所有外部流量转发到 nginx:80
  # ============================================================
  nginx:
    image: nginx:1.27-alpine
    container_name: wiki-nginx
    # 不对外暴露端口 —— Cloudflare Tunnel 通过 Docker 内部网络访问
    # 如需本地调试可临时解注释：
    # ports:
    #   - "127.0.0.1:80:80"
    expose:
      - "80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - nginx_logs:/var/log/nginx
    depends_on:
      backend:
        condition: service_healthy
      frontend:
        condition: service_started
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ============================================================
  # Cloudflare Tunnel —— 唯一对外入口
  # 通过出站连接到 Cloudflare 边缘节点，绕过阿里云端口封锁
  # 流量路径：用户 → Cloudflare CDN → Tunnel → nginx:80
  # ============================================================
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: wiki-tunnel
    command: tunnel run
    environment:
      - TUNNEL_TOKEN=${CF_TUNNEL_TOKEN:?Cloudflare Tunnel token required}
    depends_on:
      nginx:
        condition: service_started
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ============================================================
  # 自动备份服务（每日凌晨3点备份 PostgreSQL）
  # ============================================================
  backup:
    image: alpine:latest
    container_name: wiki-backup
    volumes:
      - ./backup:/backup
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_DB: ${DB_NAME:-wiki}
      PGPASSWORD: ${DB_PASSWORD:-changeme}
    command: |
      sh -c '
        apk add --no-cache postgresql-client gzip dcron
        echo "0 3 * * * pg_dump -h db -U $$POSTGRES_USER -d $$POSTGRES_DB --no-owner --no-acl | gzip > /backup/wiki_backup_\$$(date +\%Y\%m\%d_\%H\%M\%S).sql.gz && find /backup -name \"wiki_backup_*.sql.gz\" -mtime +7 -delete" > /etc/crontabs/root
        echo "[Backup] Scheduled daily at 3:00 AM"
        crond -f -l 2
      '
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  pgdata:
  uploads:
  nginx_logs:
